Set up creditials:
Copy paste your local .env to /.env

Set up env:
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

To resolve whisper error:
pip uninstall -y whisper && pip install git+https://github.com/openai/whisper.git

BE: python server/app.py

FE: python -m http.server 8000

Open http://127.0.0.1:8000/public/